<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.roseri66</groupId>
        <artifactId>metapool-parent</artifactId>
        <version>2.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>metapool-bom</artifactId>
    <packaging>pom</packaging>

    <name>MetaPool BOM</name>
    <description>MetaPool 物料清单 — 统一对齐各 metapool-* 模块版本，供使用方 import</description>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.github.roseri66</groupId>
                <artifactId>metapool-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.roseri66</groupId>
                <artifactId>metapool-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.roseri66</groupId>
                <artifactId>metapool-adapter-hikari</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.roseri66</groupId>
                <artifactId>metapool-adapter-bucket4j</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.github.roseri66</groupId>
                <artifactId>metapool-spring-starter</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
